# Security

This repository uses synthetic retention and commerce data only. Do not commit real customer data, order identifiers, API keys, store credentials, hostnames, SSH keys, FTP credentials, or internal file paths.

Before publishing changes, run `npm run verify` plus a strict local path and secret marker scan.
